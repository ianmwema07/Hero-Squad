{{#partial "content"}}

    <h1> List o' stuff to do!</h1>

    {{#if heros}}
        {{#each heros }}
            <p><a href="/squads/{{squadId}}/heroes/{{id}}">{{description}}</a></p>
        {{/each}}
    {{else}}
        <p>No heroes!</p>
    {{/if}}
{{/partial}}

{{> layout.hbs}}